#include "str.h"
#include "../../srcs/srcs.h"

void	strchr_test(t_test_lst *head)
{
	/*
	t_test_lst *cpy;

	cpy = head;
	while (cpy)
	{
		ft_putstr(ft_strchr((char const *)cpy->test.str, 'l'));
		ft_putchar('\n');
		cpy = cpy->next;
	}*/
}
