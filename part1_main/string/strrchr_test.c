#include "str.h"
#include "../../srcs/srcs.h"

void	strrchr_test(t_test_lst *head)
{
	//print_t_test_lst(head);
	/*
	while (head)
	{
		ft_putstr(ft_strrchr(head->test.str, 'l'));
		ft_putchar('\n');
		head = head->next;
	}
	*/
}